
#
# Pasos para configurar el admin:
# 1. Crear un usuario admin en Supabase Dashboard → Authentication → Users
# 2. Configurar RLS en la tabla "products":
#    - anon SELECT: USING (is_active = true)
#    - authenticated SELECT: USING (true)
#    - authenticated INSERT/UPDATE: WITH CHECK (true)
# 3. Configurar policy en Storage bucket "catalog":
#    - SELECT (download) para todos
#    - INSERT (upload) solo para authenticated

PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
